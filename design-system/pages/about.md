# Page Override: About (/about)
> Surcharge du MASTER.md pour la page À propos.

## Déviations

### Layout
- Hero section : overlay image sombre (hardcodé) + texte blanc
- Timeline verticale : ligne `var(--border)` + dot `var(--bg-2)`
- Sections non-hero : `background: var(--bg)` ou `var(--bg-2)`

### Grille timeline
- Cards avec `border: 1px solid var(--border)`
- Dot background : `var(--bg-2)` (thème-aware)
